server:
  port: 9090
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/java15x
    username: postgres
    password: ROOT
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true